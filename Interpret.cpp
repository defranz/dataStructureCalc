#include "Interpret.h"

void Interpret::parse(){
	//while expqueue not empty 

	//get an expression out of expqueue

	//tokenize expression out of expqueue

	//balance check

	//if (this expression has balanced parenthesis)
	//do

		//insert expression into the explist

	//else print error message
}